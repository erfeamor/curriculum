---
id: T-016
title: "Dev/prod parity: bump the local MySQL to 8.4"
repo: cv-project (meta)
status: todo
owner:
branch: chore/dev-mysql-84-parity
pr:
depends_on: []
risk: normal
security_review: false
---

## Why this exists

Split out of T-001 on 2026-08-13 (§2 of its original three-part scope), so a local-stack chore cannot sit in the dependency path of the deployment chain — `T-014` gates on T-001, and T-001 is now the backup alone.

`docker-compose.dev.yml` still pins `mysql:8.0` while production runs **`mysql:8.4`** on the self-hosted container. Dev and prod disagreeing on the database major version is exactly the gap that lets a migration or a query pass locally and fail deployed.

## Scope

- Bump `docker-compose.dev.yml` to `mysql:8.4`.
- **The existing `cv-dev-mysql-data` volume was created under 8.0.** Either `docker compose down -v` (dev seed data is regenerated by the Flyway callback, so this costs nothing) or rely on MySQL's in-place 8.0→8.4 upgrade. Whichever is chosen, write it into the repo docs — the next person to hit a dirty volume should not have to rediscover it.
- Re-run the local stack and the E2E smoke.

## Acceptance criteria

- [ ] `docker-compose.dev.yml` runs `mysql:8.4`.
- [ ] Full local stack comes up clean **from an empty volume**, Flyway migrates, and the dev-seed callback populates.
- [ ] The E2E smoke passes end to end. **Note the BFF path moved** — since T-202 the BFF serves `/bff/api/v1`, so the smoke is `curl localhost:3000/bff/api/v1/people/1`, not the `/api/v1` form still written in the meta CLAUDE.md.
- [ ] The volume-upgrade gotcha is documented wherever the dev-stack commands live.

## Definition of done

PR open against `master` from `chore/dev-mysql-84-parity`, local stack verified, task updated.

## dev-loop notes

- **Developer:** `infrastructure-engineer` (compose/stack config). **Reviewers:** `/code-review`.
- `security_review: false` — no auth, IAM, secrets or CI surface.
- This one genuinely needs the stack run, not just a config read: the whole point is that 8.4 starts cleanly against a real volume.
